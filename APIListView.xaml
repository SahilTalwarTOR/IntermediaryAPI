<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="vs_iAPI.APIListView"
             Title="View APIs">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label x:Name="apilistName"
          Text="iAPI List"
           Grid.Row="0"
           Grid.Column="1"
           HorizontalTextAlignment="Center"
           FontSize="32"
          />

        <VerticalStackLayout Grid.Row="0" Padding="24" Spacing="16">
            <ListView x:Name="apiList">
                
            </ListView>
        </VerticalStackLayout>

    </Grid>
</ContentPage>